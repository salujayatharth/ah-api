# Environment Variables for AH API

# Data directory (where SQLite DB and tokens are stored)
# Default: /data (in Docker) or current directory (local dev)
DATA_DIR=/data

# Note: Authentication tokens are managed automatically via /receipts/auth endpoint
# No manual token configuration needed - just authenticate via the API
